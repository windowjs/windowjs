<a id="{{include.class}}{{include.object}}.{{include.name}}" class="api"><span class="api-left">{% if include.class != "" %}<span class="code-class">{{ include.class }}</span>{% endif %}{% if include.object != "" %}<span class="code-object">{{ include.object }}</span>{% endif %}.<span class="code-name">{{ include.name }}</span></span><span class="api-right">{% if include.type != "" %}<span class="code-type">{{ include.type | escape }}</span>{% endif %}</span></a>

